---
name: Welcome
route: /
order: 1
---

import { Tree, Node } from './components';

# react-svg-tree

<Tree width={200} height={75} rootId="A" showLabels={true}>
  <Node id="A" childNodes={['B', 'C', 'D']} />
  <Node id="B" childNodes={['E', 'F']} />
  <Node id="C" childNodes={['G', 'H']} />
  <Node id="D" childNodes={['I']} />
  <Node id="E"/>
  <Node id="F"/>
  <Node id="G"/>
  <Node id="H"/>
  <Node id="I"/>
</Tree>

`react-svg-tree` is a package that renders an svg tree from a data structure of nodes. It can also optionally render the tree from children that are provided at render. The package exposes two components, `Tree` and `Node`, which are documented on their respective pages.